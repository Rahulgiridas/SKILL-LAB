class Solution(object):
    def removeDuplicates(self, nums):
        ind=0
        for i in range(1,len(nums)):
            if nums[ind]!=nums[i]:
                ind=ind+1
                numsind=nums[i]
        return ind+1      
